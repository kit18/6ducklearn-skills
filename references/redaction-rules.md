# Redaction rules

Before publishing or syncing artifacts:
- remove secrets, tokens, and credentials
- remove personal identifiers unless the user opted in
- keep only the minimum text needed for useful session recall
- default to private when visibility is unclear
- require approval for public or shared publishing paths
